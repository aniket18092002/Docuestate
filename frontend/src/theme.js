// src/theme.js
import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  // drop in overrides or palettes as needed
  palette: {
    mode: "dark",
  },
});

export default theme;
